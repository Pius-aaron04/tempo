{
  "name": "tempo-vscode",
  "displayName": "TempoExtension",
  "description": "Local-first developer activity tracker",
  "version": "0.0.2",
  "publisher": "SudoTechAsAtechbro",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/pius-aaron04/tempo"
  },
  "icon": "icon.png",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {},
  "scripts": {
    "vscode:prepublish": "pnpm run bundle",
    "bundle": "esbuild src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --format=cjs --platform=node",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package --no-dependencies",
    "pretest": "pnpm run bundle && pnpm run lint",
    "lint": "eslint src",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.80.0",
    "esbuild": "^0.27.4",
    "typescript": "^5.9.3"
  },
  "packageManager": "pnpm@10.21.0",
  "dependencies": {
    "@tempo/contracts": "workspace:*",
    "zod": "^4.3.5"
  }
}
